module NativeUi.Enum.StatusBar (..) where

{-| A node in the virtual View Tree that forms the basis of the UI for your app.
-}


type BarStyle
  = Default
  | LightContent


{-| A node in the virtual View Tree that forms the basis of the UI for your app.
-}
type ShowHideTransition
  = Fade
  | Slide
